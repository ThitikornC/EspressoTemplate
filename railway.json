{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci --prefer-offline --no-audit --no-fund && cd classroomv3-main && npm ci --prefer-offline --no-audit --no-fund && npm run build"
  },
  "deploy": {
    "startCommand": "NODE_ENV=production node server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
