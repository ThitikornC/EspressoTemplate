# 🎨 ห้องเรียนสนุก - Classroom Games

เว็บแอปพลิเคชันสำหรับห้องเรียนที่รองรับจอสัมผัส (Touch Screen / Smartboard / Projector) 
มีเกมระบายสี (Coloring Game) และเกมจิ๊กซอว์ (Puzzle Game) พร้อมระบบหลังบ้านสำหรับครู

## ✨ ฟีเจอร์หลัก

### 🎨 เกมระบายสี (Coloring Game)
- วาดและระบายสีด้วยนิ้วหรือปากกา stylus
- เลือกสีจากพาเลท 20 สี
- ปรับขนาดพู่กัน (2-50px)
- ยางลบและ Undo/Redo
- เปิด/ปิดแสดงเส้นขอบภาพ
- บันทึกภาพเป็น PNG

### 🧩 เกมจิ๊กซอว์ (Puzzle Game)
- ตัดรูปภาพอัตโนมัติเป็นชิ้นส่วน
- 3 ระดับความยาก: ง่าย (3×3), ปานกลาง (4×4), ยาก (5×5)
- ลากวางชิ้นส่วนด้วย Drag & Drop (รองรับ touch)
- ระบบคำใบ้ (Hint) แสดงตำแหน่งที่ถูกต้อง
- แอนิเมชันและเสียงเมื่อต่อสำเร็จ

### 🎯 เกมโยงเส้นจับคู่ (Matching Line Game)
- ลากเส้นแบบ freehand เพื่อเชื่อมรูปภาพที่เข้าคู่กัน
- 3 ระดับความยาก: ง่าย (3 คู่), ปานกลาง (5 คู่), ยาก (7 คู่)
- ตรวจสอบคำตอบแบบ real-time
- เส้นถูกเป็นสีเขียว + ล็อก + เสียงถูก
- เส้นผิดเป็นสีแดงแล้วหายไป + เสียงผิด
- รองรับ touch และ stylus บน smartboard/tablet

### 🎮 เกมลูกปัดลอย (Floating Beads Game)
**โหมดแตะตามลำดับ:**
- ลูกปัดลอยด้วย animation
- แตะตามหมายเลขที่กำหนด
- แตะถูกมี effect + เสียงถูก
- แตะผิดลูกปัดสั่น + เสียงผิด

**โหมดลากใส่ตะกร้า:**
- ลากลูกปัดเข้าตะกร้าที่มีสีตรงกัน
- ตรวจสอบคะแนนแบบ real-time
- 4 สีตะกร้า (แดง, น้ำเงิน, เขียว, เหลือง)

### 👩‍🏫 หน้าครู (Teacher Dashboard)
- สร้างและจัดการกิจกรรม
- อัปโหลดและจัดการคลังรูปภาพ
- ดูและบันทึกคะแนนนักเรียน
- Export ข้อมูลเป็น CSV

## 🛠️ เทคโนโลยีที่ใช้

**Frontend:**
- React 18 + TypeScript
- Vite (Build tool)
- React Konva (Canvas drawing)
- React DnD (Drag & Drop)
- Zustand (State management)
- Web Audio API (Sound effects)

**Backend:**
- Node.js + Express
- TypeScript
- Multer (File upload)
- Sharp (Image processing)

## 🚀 วิธีรันโปรเจค

### ข้อกำหนดเบื้องต้น
- Node.js 18+ 
- npm หรือ yarn

### 1. ติดตั้ง Dependencies

**Frontend:**
```powershell
npm install
```

**Backend:**
```powershell
cd backend
npm install
```

### 2. รันโปรเจค

**วิธีที่ 1: รันทั้ง Frontend และ Backend พร้อมกัน**

เปิด Terminal 2 หน้าต่าง:

**Terminal 1 (Frontend):**
```powershell
npm run dev
```
Frontend จะรันที่ http://localhost:3000

**Terminal 2 (Backend):**
```powershell
cd backend
npm run dev
```
Backend จะรันที่ http://localhost:5000

**วิธีที่ 2: ใช้คำสั่งเดียว (แนะนำ)**

ติดตั้ง concurrently ก่อน:
```powershell
npm install --save-dev concurrently
```

จากนั้นเพิ่มใน `package.json`:
```json
"scripts": {
  "dev:all": "concurrently \"npm run dev\" \"cd backend && npm run dev\""
}
```

แล้วรัน:
```powershell
npm run dev:all
```

### 3. เปิดเว็บไซต์

เข้าที่ http://localhost:3000 ในเบราว์เซอร์

## 📁 โครงสร้างโปรเจค

```
classroom-games/
├── src/                      # Frontend source
│   ├── components/           # React components
│   │   ├── Toolbox.tsx      # เครื่องมือวาดภาพ
│   │   ├── PuzzleBoard.tsx  # กระดานจิ๊กซอว์
│   │   ├── MatchingBoard.tsx # กระดานโยงเส้นจับคู่
│   │   └── FloatingBeadsBoard.tsx # กระดานลูกปัดลอย
│   ├── pages/               
│   │   ├── HomePage.tsx     # หน้าหลัก
│   │   ├── Student/         # หน้านักเรียน
│   │   │   ├── PlayColoring.tsx
│   │   │   ├── PlayPuzzle.tsx
│   │   │   ├── PlayMatching.tsx
│   │   │   └── PlayBeads.tsx
│   │   └── Teacher/         # หน้าครู
│   │       └── Dashboard.tsx
│   ├── services/
│   │   └── api.ts           # API calls
│   ├── store/
│   │   └── appStore.ts      # Zustand store
│   └── utils/
│       └── audio.ts         # Sound manager
│
├── backend/                  # Backend source
│   └── src/
│       └── server.ts        # Express server
│
├── package.json
├── vite.config.ts
└── tsconfig.json
```

## 🎮 วิธีใช้งาน

### สำหรับนักเรียน

1. **หน้าหลัก**: เลือกเกมที่ต้องการเล่น (ระบายสี, จิ๊กซอว์, โยงเส้นจับคู่, หรือลูกปัดลอย)

2. **เกมระบายสี**:
   - อัปโหลดรูปภาพหรือใช้รูปตัวอย่าง
   - เลือกสีจากพาเลท
   - ปรับขนาดพู่กัน
   - วาดด้วยนิ้วหรือ stylus
   - กดบันทึกเมื่อเสร็จ

3. **เกมจิ๊กซอว์**:
   - อัปโหลดรูปภาพ
   - เลือกระดับความยาก
   - ลากวางชิ้นส่วนให้ถูกที่
   - ใช้คำใบ้หากต้องการ

4. **เกมโยงเส้นจับคู่**:
   - เลือกระดับความยาก (3, 5, หรือ 7 คู่)
   - ลากเส้นจากรูปซ้ายไปรูปขวาที่เข้าคู่กัน
   - เส้นสีเขียว = ถูก, เส้นสีแดง = ผิด
   - จับคู่ให้ครบทุกคู่

5. **เกมลูกปัดลอย**:
   - **โหมดแตะตามลำดับ**: แตะลูกปัดตามหมายเลข 1, 2, 3...
   - **โหมดลากใส่ตะกร้า**: ลากลูกปัดสีแดงใส่ตะกร้าแดง, สีน้ำเงินใส่ตะกร้าน้ำเงิน ฯลฯ

### สำหรับครู

1. **เข้าหน้าครู**: คลิกปุ่ม "👩‍🏫 หน้าครู" ที่มุมบนขวา

2. **สร้างกิจกรรมใหม่**:
   - คลิก "➕ สร้างกิจกรรมใหม่"
   - กรอกชื่อกิจกรรม
   - เลือกประเภทเกม
   - อัปโหลดรูปภาพ
   - กดสร้างกิจกรรม

3. **ดูคะแนนนักเรียน**:
   - ดูตารางคะแนนในหน้า Dashboard
   - Export เป็น CSV สำหรับนำไปใช้ต่อ

## 🎨 UX/UI Features

- **ธีมสีสดใส**: เหมาะสำหรับเด็ก (ฟ้า-เขียว-เหลือง)
- **ปุ่มใหญ่**: min 48px สำหรับจอสัมผัส
- **ฟอนต์ไทย**: Noto Sans Thai อ่านง่าย
- **เสียงตอบสนอง**: เสียงกดปุ่มและเสียงสำเร็จ
- **ตัวการ์ตูน**: มีมาสคอตน่ารักคอยช่วยเหลือ
- **Responsive**: ใช้งานได้ทั้งมือถือและจอใหญ่

## 🔧 การปรับแต่ง

### เปลี่ยนสีธีม
แก้ไขใน `src/index.css`:
```css
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
```

### เพิ่มสีในพาเลท
แก้ไขใน `src/components/Toolbox.tsx`:
```typescript
const COLORS = [
  '#FF6B6B', '#4ECDC4', // เพิ่มสีที่นี่
]
```

### ปรับระดับความยากของจิ๊กซอว์
แก้ไขใน `src/components/PuzzleBoard.tsx`:
```typescript
const GRID_SIZE = {
  easy: 3,    // เปลี่ยนเป็น 2 หรือ 4
  medium: 4,  
  hard: 5
}
```

## 📝 API Endpoints

```
POST   /api/upload-image      - อัปโหลดรูปภาพ
POST   /api/slice-image       - สร้าง config สำหรับ puzzle
GET    /api/gallery           - ดึงรายการรูปทั้งหมด
POST   /api/activity          - สร้างกิจกรรมใหม่
GET    /api/activities        - ดึงรายการกิจกรรมทั้งหมด
GET    /api/activity/:id/results - ดึงผลคะแนนของกิจกรรม
POST   /api/submit            - ส่งผลงานนักเรียน
GET    /api/health            - ตรวจสอบสถานะ server
```

## 🐛 การแก้ปัญหา

**ปัญหา: Frontend ไม่เชื่อมต่อ Backend**
- ตรวจสอบว่า Backend รันอยู่ที่ port 5000
- ตรวจสอบ CORS settings ใน `backend/src/server.ts`

**ปัญหา: รูปภาพอัปโหลดไม่ได้**
- ตรวจสอบว่ามีโฟลเดอร์ `backend/uploads/`
- ตรวจสอบขนาดไฟล์ (limit 10MB)

**ปัญหา: Drag & Drop ใช้งานไม่ได้บนมือถือ**
- ตรวจสอบว่าได้ใช้ TouchBackend สำหรับอุปกรณ์สัมผัส

## 🚀 การ Deploy

### Frontend (Vercel/Netlify)
```powershell
npm run build
# Upload โฟลเดอร์ dist/ ไปยัง Vercel หรือ Netlify
```

### Backend (Render/Railway)
```powershell
cd backend
npm run build
# Deploy โฟลเดอร์ backend/ ไปยัง Render หรือ Railway
```

## 📄 License

MIT License - ใช้งานได้อย่างอิสระ

## 👨‍💻 ผู้พัฒนา

สร้างด้วย ❤️ สำหรับการศึกษา

---

**หมายเหตุ**: โปรเจคนี้เป็น demo version ที่ใช้ in-memory storage 
สำหรับการใช้งานจริงควรเชื่อมต่อกับฐานข้อมูล (PostgreSQL/MongoDB)
#   c l a s s r o o m 2  
 #   c l a s s r o o m v 3  
 